# 版本更新记录

本文档统一记录 5-dof 主仓库与 `wa100-sdk-publish` 子仓库的主要版本变化。

- 5-dof 仓库：机械臂、灵巧手、相机、控制 UI、数据记录与辅助监控。
- WA100 子仓库：Unity UDP 接收、手套映射、拇指标定及 WA100 反馈处理。
- 日期采用提交日期；提交号可用于定位、比较和回退。
- 正式运行版本以两个仓库各自的 `main` 分支为准。

## 当前正式版本

### 2026-08-11：原始输入记录与遥操作控制正式并入主线

| 仓库 | 功能基线提交 | 说明 |
| --- | --- | --- |
| 5-dof | `9f8d8c7` | 合并主手腕部融合、手部通道布局、相机回退与低延迟推流、原始输入记录与遥操作控制；子模块指向 WA100 `d091745` |
| WA100 | `d091745` | 集成手套-手柄对齐、统一手部 ID 与手指死区补偿、原始手部输入遥测 |

本版本的主要更新：

- 增加原始输入（pre-mapping master input）记录：按当前控制方法（臂侧键盘/手柄/手部视觉/手柄增量 + 手侧 VR/数据手套）只记录活跃输入源的原始样本到 JSONL，非活跃来源拒绝并计数，保证实验数据来源可追溯。
- 增加浏览器原始输入 WebSocket 通道（`/raw-input`）与遥操作控制界面。
- 增加 WA100 本地运动学模型（URDF 编译 + 标定表 + 运行时正运动学），运行时不依赖 ROS/STL。
- 主手采用右手手柄 + Hi5 手套腕部融合：手柄保留平移与控制权，Hi5 仅贡献短期姿态增量。
- 记录服务扩展同步多模态记录（相机帧侧车索引、融合遥测、原始主手输入）。
- 双路相机支持设备名回退匹配（精确 → 大小写不敏感子串）与低延迟 latest-frame 推流。
- 统一手部 ID 通道布局并补偿手指死区。

验证结果：

- 5-dof：120 项 Python 测试通过，3 项环境相关测试跳过。
- WA100：7 项 Python 测试通过；`wa100_channel_layout_test`、`wa100_feedback_filter_test`、`glove_thumb_mapper_test` 3 项 C++ 测试通过。
- `udp_receiver_unity.exe` 等 WA100 目标构建成功。

### 2026-07-23（上一正式版本）：臂手控制与拇指映射正式并入主线

| 仓库 | 功能基线提交 | 说明 |
| --- | --- | --- |
| 5-dof | `d1830e8` | 合并双模式臂手控制、相机模式、数据记录和一致性辅助；子模块指向 WA100 `df37c9d` |
| WA100 | `df37c9d` | 将 11 个实验提交压缩为一个正式版本，集成 Unity 接收、拇指映射/标定和反馈过滤 |

本版本的主要更新：

- 完成 VR/Unity、手套、机械臂和灵巧手控制链路的整合。
- 增加相机启动模式、双路 RTSP 画面和控制 UI 配置。
- 增加遥操作 episode 记录、回放和 `ArmRecordingState` 状态。
- 增加感知/一致性辅助监控及其状态协议。
- 拇指采用固定手腕、单传感器可用的横摇/弯曲映射基线。
- 拇指标定 JSON 默认静态加载，在线热更新不再默认启用。
- WA100 反馈中位置无效时，电流有效性可独立判断，避免一致性辅助因零位附近的 `invalid position` 失效。
- 临时辨识脚本、临时标定工具、采集数据和构建产物继续保留在本地，不进入 Git。
- WA100 曾在实验历史中加入后又删除的大型 CSV 没有进入压缩后的 `main` 历史。

验证结果：

- 5-dof：74 项 Python 测试通过，2 项环境相关测试跳过。
- WA100：5 项 Python 测试、2 项 C++ 测试通过。
- `udp_receiver_unity.exe` 等 WA100 目标构建成功。

## 5-dof 主仓库版本

### 2026-08-11：`9f8d8c7` — 原始输入记录与遥操作控制

- 增加原始主手输入（keyboard/gamepad/pico_hand/data_glove/vr_controller）校验与 JSONL 记录，仅记录当前控制方法涉及的活跃来源，非活跃来源按原因拒绝并计数。
- H5 UI 与 bridge 增加遥操作控制与 `/raw-input` WebSocket 通道。
- 增加 WA100 本地运动学模型（`wa100_kinematics.py`、`wa100_kinematics_calibration.json`、`wa100_kinematics_model.json`）及 `WA100_KINEMATICS.md` 文档。
- 记录服务扩展原始输入记录与浏览器连接状态；更新 `Hand_Tracker` 输入遥测发布。
- 新增 `test_raw_input_validation`、`test_wa100_kinematics`、`test_ui_teleop_control` 等测试。
- 更新 WA100 子模块至 `d091745`。

### 2026-08-05：`6af3262` — 相机回退与低延迟推流（合并 `ae1a3de`）

- 相机设备名支持多候选回退匹配：先精确匹配，再按 `|` 分隔做大小写不敏感子串匹配（如 `USB Camera|DSJ-2062-309`）。
- latest-frame 后端低延迟推流：30 fps 采集、20 fps 发布，支持 MJPG 输入与 CRF 配置。
- 重构 `start_dual_rtsp_camera.ps1` 启动流程。

该提交通过合并提交 `ae1a3de` 进入 `main`。

### 2026-07-31：`5d578a3` — WA100 子模块更新

- 子模块指向 WA100 合并后的 `main`（`99ad4de`）。

### 2026-07-31：`b47f9f2` — 手部通道布局与腕部相机朝向（合并 `97cc86b`）

- 统一手部通道布局（hand channel layout），新增 `test_hand_channel_layout`。
- 腕部相机画面朝向对齐（镜像/翻转），调整双相机拼接、感知辅助配置与记录服务。

该提交通过合并提交 `97cc86b` 进入 `main`。

### 2026-07-27：`40f9c90` — 主手腕部融合与同步记录

- 增加 `Hand_Tracker/master_fusion.py`：右手手柄 + Hi5 手套腕部融合，手柄保留平移与控制权，Hi5 仅贡献短期姿态增量。
- 记录服务支持同步多模态记录：相机帧侧车索引、融合遥测与原始主手输入。
- 增加手部控制配置与 `test_master_fusion` 等测试。
- 增加臂手协同调研与实验大纲文档（`docs/`）。
- 更新 WA100 子模块至 `76e5109`。

### 2026-07-23：`4c6cd39` — 臂手控制、记录与辅助监控

- 增加遥操作数据记录服务和 episode 回放。
- 增加机械臂记录状态消息及实际电流测试。
- 增加感知/一致性辅助监控、配置和状态 JSON Schema。
- 更新 H5 UDP bridge、机械臂节点、控制 UI 和 Jetson 部署配置。
- 增加双路相机最新帧处理。
- 更新 WA100 子模块版本。

该提交通过合并提交 `d1830e8` 进入 `main`。

### 2026-07-20：`aae8c4f` — 相机模式与控制链路整理

- 整理相机启动模式和控制 UI。
- 整合 Unity/手套/机械臂控制入口。
- 更新一键启动流程和子模块配置。
- 移除旧的延迟探针、延迟报告及对应测试。

该提交通过合并提交 `d1830e8` 进入 `main`。

### 2026-07-14：`dfc9e73` — 100 Hz 关节规划与运动日志

- 增加 100 Hz 关节轨迹规划。
- 增加机械臂运动过程日志，支持运行状态分析。

### 2026-07-13：`05cc4da` — 双模式机械臂控制与延迟追踪

- 合并双模式机械臂控制。
- 增加控制链路延迟追踪能力。

### 2026-07-02：`a8dc180` — Unity 手腕使能逻辑

- 使用 Unity 手腕状态作为控制使能条件。
- 调整手腕 deadman 逻辑。

### 2026-07-01：`2116944` — 臂手控制仲裁

- 修复机械臂与灵巧手控制源之间的仲裁。
- 降低多个输入源同时存在时的控制冲突。

### 2026-06-30：`53b753e` — 手柄增量遥操作

- 增加 VR 手柄位姿增量控制模式。
- 为后续手柄与手套融合提供机械臂遥操作基础。

### 2026-06-26：`42f2426` — 接口与延迟优化

- 规范控制接口。
- 减少跨设备控制链路延迟。

### 2026-06-22：`758bc1d`、`9994a8b` — 相机工作流与 WA100 更新

- 更新控制 UI 的相机串流工作流。
- 更新 WA100 SDK 子模块指针。

### 2026-06-16：`09912fb`、`6e38b99`、`0479e46`

- 更新 WA100 SDK 子模块。
- 增加 USB 相机支持。
- 调整控制页面数据长度和显示。

### 2026-06-15：`67b71a0`、`dfc2095`

- 增加基于 VR 的位置控制。
- 增加外部相机接入。

### 2026-06-12：`8eea9b2`、`264c99d`、`1532e28`

- 增加 VR 控制入口。
- 修复 VR 灵巧手控制。
- 将控制网页同步显示到头显。

### 2026-06-10：`a509abc` — 手部追踪与相机基础

- 增加手部追踪程序。
- 建立相机和手部控制的基础链路。

## WA100 子仓库版本

### 2026-08-11：`d091745` — 原始手部输入遥测

- `udp_receiver_unity.cpp` 增加原始手部输入遥测（raw hand input telemetry）记录。

### 2026-07-31：`99ad4de` — 统一手部 ID 与手指死区补偿（合并 `95124e1`）

- 新增 `wa100_channel_layout.h` 统一手部 ID 通道布局，并增加对应 C++ 测试。
- 补偿手指死区，更新相机、日志回放与拇指映射回放示例。

该提交通过合并提交 `95124e1` 进入 `main`。

### 2026-07-27：`76e5109` — 手套-手柄对齐与融合遥测

- 增加手套-手柄对齐标定程序及 GUI（`glove_controller_alignment.py`、`glove_controller_alignment_gui.py`）。
- 增加手指端点标定与 Hi5 语义回放工具。
- `udp_receiver_unity.cpp` 增加融合遥测输出。
- 新增 `test_glove_controller_alignment` 测试与 `GLOVE_CONTROLLER_ALIGNMENT.md` 文档。

### 2026-07-23：`df37c9d` — 正式主线集成

该提交是下面 11 个实验提交的压缩结果。功能已进入 `main`，原实验提交仍保留在
`agent/glove-thumb-quaternion-calibration` 分支中。

- 集成 Unity UDP 接收与 WA100 控制。
- 集成手套拇指横摇、弯曲映射。
- 支持固定手腕、单拇指传感器的标定流程。
- 增加静态标定 JSON、离线回放和映射测试。
- 增加 WA100 反馈有效性过滤和测试。
- 默认关闭高频接收日志。

### 2026-07-23：`8e9b09b` — 稳定拇指映射与 WA100 反馈

- 固化当前拇指横摇与弯曲映射行为。
- 增加 WA100 反馈过滤模块。
- 位置无效时独立保留有效电流。
- 增加反馈过滤和拇指映射自动测试。
- 在线热更新改为显式启用，正常运行默认读取静态 JSON。

### 2026-07-22：`9dc213c` — 固定手腕、单传感器拇指标定

- 针对拇指只有一个有效传感器的情况调整标定。
- 暂不消除手腕旋转影响，要求标定和使用时固定手腕。
- 提供图形化标定入口。

回退标签：`glove-thumb-fixed-wrist-baseline-20260722`。

### 2026-07-22：`b760e5a` — 标定式拇指 Rotation 映射

- 将拇指映射拆分为独立计算模块。
- 增加旋转映射、标定 JSON 和离线回放。
- 增加合成标定数据和单元测试。

### 2026-07-22：`89a2ec2` — 拇指 Rotation 基线

- 保存优化前的手套 Rotation 映射运行状态。
- 建立可直接回退的拇指映射基线。

回退标签：`glove-thumb-baseline-20260722`。

### 2026-07-20：`9aa827b` — Unity UDP 映射调整

- 调整 Unity 手套数据到 WA100 目标关节的映射。
- 更新本地生成文件忽略规则。

### 2026-07-13：`15490c4` — WA100 拇指参数调整

- 调整拇指横摇和弯曲的映射参数。
- 该版本曾作为 5-dof 主仓库的 WA100 子模块版本。

### 2026-07-01：`0b7499a` — 默认关闭接收日志

- 默认关闭 WA100 UDP 接收器的高频日志。
- 保留按需启用日志的能力。

### 2026-06-22：`4f37888` — Unity 接收与日志整理

- 更新 Unity UDP 接收示例。
- 整理运行日志处理。
- 删除实验阶段产生的大型 CSV 文件。

### 2026-06-16：`b458798` — VR 手部追踪标定

- 增加 VR 手部追踪标定程序。
- 增加 Unity 接收端对应的数据入口。

### 2026-04-08：`64793ac` — WA100 控制原型

- 增加早期 Unity UDP 接收器。
- 增加服务程序、拇指标定原型和快速驱动调整。
- 该实验提交曾包含大型日志 CSV；正式 `main` 采用 squash，因此不包含该中间文件历史。

### 2026-03-09：`dd397e6` — 相机程序调整

- 调整早期相机采集程序。

## 分支与回退点

| 用途 | 仓库 | 分支或标签 | 当前提交 |
| --- | --- | --- | --- |
| 正式主线功能基线 | 5-dof | `main` | `9f8d8c7` |
| 合并前开发历史 | 5-dof | `agent/dual-mode-arm-motion` | `4c6cd39` |
| 正式主线功能基线 | WA100 | `main` | `d091745` |
| 完整实验历史 | WA100 | `agent/glove-thumb-quaternion-calibration` | `8e9b09b` |
| Rotation 原始基线 | WA100 | `glove-thumb-baseline-20260722` | `89a2ec2` |
| 固定手腕单传感器基线 | WA100 | `glove-thumb-fixed-wrist-baseline-20260722` | `9dc213c` |

## 后续记录规则

新增正式版本时，在本文档顶部追加一节，至少记录：

1. 日期、仓库和提交号。
2. 操作行为或数据接口发生的变化。
3. 标定文件、启动参数或部署位置是否变化。
4. 已执行的自动测试和实机验证。
5. 可用的回退提交或标签。
